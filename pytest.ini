[pytest]
testpaths = backend/tests
markers =
    browser: drives real Chrome against a running ACE. Skipped when
        Chrome or the server is absent, so a plain checkout stays green.
        Run only these with: pytest -m browser
